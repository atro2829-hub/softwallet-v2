import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.softwallet.user',
  appName: 'سوفت واليت',
  webDir: 'out',
  server: {
    androidScheme: 'https',
  },
};

export default config;
